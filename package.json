{
    "name": "smashmart-root",
    "version": "1.0.0",
    "description": "Root script to run both frontend and backend",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
        "seed": "cd backend && node seed-native.js"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}